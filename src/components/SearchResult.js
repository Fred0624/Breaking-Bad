import React from 'react';

const SearchResult = ({searchCharcter}) => {
    return (
        <div>
            <p>{searchCharcter.name}</p>
            
        </div>
    );
};

export default SearchResult;